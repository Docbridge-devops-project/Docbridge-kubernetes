apiVersion: v1
kind: Service
metadata:
  name: reminder-service-svc
  namespace: production
spec:
  type: ClusterIP
  ports:
    - port: 3004
      targetPort: 3004
      protocol: TCP
  selector:
    app: reminder-service
