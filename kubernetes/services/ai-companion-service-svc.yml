apiVersion: v1
kind: Service
metadata:
  name: ai-companion-service-svc
  namespace: production
spec:
  type: ClusterIP
  ports:
    - port: 3007
      targetPort: 3007
      protocol: TCP
  selector:
    app: ai-companion-service
