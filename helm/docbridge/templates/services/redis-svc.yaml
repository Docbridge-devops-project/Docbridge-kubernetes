apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: production
spec:
  ports:
    - port: {{ .Values.redis.port }}
      targetPort: {{ .Values.redis.port }}
      protocol: TCP
  selector:
    app: redis
  type: ClusterIP
