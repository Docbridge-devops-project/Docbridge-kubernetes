apiVersion: v1
kind: Service
metadata:
  name: family-service-svc
  namespace: production
spec:
  type: ClusterIP
  ports:
    - port: 3009
      targetPort: 3009
      protocol: TCP
  selector:
    app: family-service
