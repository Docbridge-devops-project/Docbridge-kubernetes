apiVersion: v1
kind: Service
metadata:
  name: labreport-service-svc
  namespace: production
spec:
  type: ClusterIP
  ports:
    - port: 3005
      targetPort: 3005
      protocol: TCP
  selector:
    app: labreport-service
