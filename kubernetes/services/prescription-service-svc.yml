apiVersion: v1
kind: Service
metadata:
  name: prescription-service-svc
  namespace: production
spec:
  type: ClusterIP
  ports:
    - port: 3003
      targetPort: 3003
      protocol: TCP
  selector:
    app: prescription-service
